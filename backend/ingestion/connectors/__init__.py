"""Data-source connectors.

For now this package holds only the `validate_*.py` credential smoke tests
(one per external source). Real ingestion connectors land here as they are
built; see `docs/data-sources.md` for what each source provides.
"""
